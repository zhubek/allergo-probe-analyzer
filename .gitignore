# Python
.venv/
__pycache__/
*.pyc

# Large source data — NOT committed (full-res medical images, archives)
снимки с разметками/
разметка/
*.zip
*.db
# ...but the small ground-truth annotations database IS tracked.
!data/labels.db

# WSL download metadata
*.Zone.Identifier
*:Zone.Identifier

# Generated detection outputs (overlays / per-image CSVs live next to source images)
*_overlay.jpg
*_clusters.jpg
*_dots.csv
*_clusters.csv
clusters_summary.csv

# Data: source images (medical data) and image zip backups.
# data/labels.db IS committed (it's small sample data + the ground truth).
data/images/
data/*.zip

# Trained models: classifier.joblib and thresholds.json ARE committed.
# Training artifacts (split + sweep CSVs) are not.
models/test_split.json
models/grid_results.csv
models/threshold_sweep_*.json

# Demo / labeled output images generated by manual API calls
demo_*.jpg
labeled_*.jpg

# OS / editor
.DS_Store

# Claude Code harness local state
.claude/
